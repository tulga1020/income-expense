import { Twentyfifth } from "@/contextcomponent/Twentyfifth";
export const Twentyfour = (props) => {
  const { ssname } = props;
  return (
    <div>
      <Twentyfifth ssname={ssname} />
    </div>
  );
};
