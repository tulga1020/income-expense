import { Twentyfour } from "@/contextcomponent/Twentyfour";
export const Twentythree = (props) => {
  const { ssname } = props;
  return (
    <div>
      <Twentyfour ssname={ssname} />
    </div>
  );
};
